// 1. Create a new UserContext
import { createContext } from 'react';

// Initialize the context
const UserContext = createContext();

// Export it so other components can use it
export default UserContext;
